class VinService {
    // static async sendLoginData({ email, password }) {
    //     await API.post('User/Login', {
    //         name: email,
    //         password
    //     })
    //         .then((response) => {
    //             localStorage.setItem('token', response.data);
    //             return response;
    //         })
    //         .catch((error) => {
    //             throw new Error(error);
    //         });
    // }
}

export default VinService;
